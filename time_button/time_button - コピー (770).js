function exec770(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output770").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor249( newColor ) {
      document.getElementById('f033').style.backgroundColor = newColor;
    }

    function changeBoxColors249( newColor ) {
      document.getElementById('drop202').style.backgroundColor = newColor;
    }
