function exec177(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output177").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor58( newColor ) {
    document.getElementById('b008').style.backgroundColor = newColor;
  }

  function changeBoxColors58( newColor ) {
    document.getElementById('drop44').style.backgroundColor = newColor;
  }
