function exec718(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output718").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor232( newColor ) {
      document.getElementById('e031').style.backgroundColor = newColor;
    }

    function changeBoxColors232( newColor ) {
      document.getElementById('drop167').style.backgroundColor = newColor;
    }
