function exec387(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output387").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor126( newColor ) {
    document.getElementById('g017').style.backgroundColor = newColor;
  }

  function changeBoxColors126( newColor ) {
    document.getElementById('drop221').style.backgroundColor = newColor;
  }
