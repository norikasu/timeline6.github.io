function exec418(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output418").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor136( newColor ) {
    document.getElementById('a019').style.backgroundColor = newColor;
  }

  function changeBoxColors136( newColor ) {
    document.getElementById('drop19').style.backgroundColor = newColor;
  }
