function exec848(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output848").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor275( newColor ) {
    document.getElementById('h036').style.backgroundColor = newColor;
    }

    function changeBoxColors275( newColor ) {
      document.getElementById('drop276').style.backgroundColor = newColor;
      }
