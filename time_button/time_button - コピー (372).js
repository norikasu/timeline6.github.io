function exec372(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output372").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor121( newColor ) {
    document.getElementById('b017').style.backgroundColor = newColor;
  }

  function changeBoxColors121( newColor ) {
    document.getElementById('drop52').style.backgroundColor = newColor;
  }
