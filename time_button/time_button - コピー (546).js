function exec546(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output546").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor176( newColor ) {
      document.getElementById('b024').style.backgroundColor = newColor;
    }

    function changeBoxColors176( newColor ) {
      document.getElementById('drop59').style.backgroundColor = newColor;
    }
