function exec497(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output497").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor159( newColor ) {
    document.getElementById('a022').style.backgroundColor = newColor;
  }

  function changeBoxColors159( newColor ) {
    document.getElementById('drop22').style.backgroundColor = newColor;
  }
