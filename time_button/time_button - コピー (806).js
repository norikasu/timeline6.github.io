function exec806(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output806").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor261( newColor ) {
      document.getElementById('b035').style.backgroundColor = newColor;
    }

    function changeBoxColors261( newColor ) {
      document.getElementById('drop70').style.backgroundColor = newColor;
    }
