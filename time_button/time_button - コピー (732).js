function exec732(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output732").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor236( newColor ) {
      document.getElementById('a032').style.backgroundColor = newColor;
    }

    function changeBoxColors236( newColor ) {
      document.getElementById('drop32').style.backgroundColor = newColor;
    }
