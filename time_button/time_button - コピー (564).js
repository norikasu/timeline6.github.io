function exec564(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output564").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor182( newColor ) {
      document.getElementById('h024').style.backgroundColor = newColor;
    }

    function changeBoxColors182( newColor ) {
      document.getElementById('drop264').style.backgroundColor = newColor;
    }
