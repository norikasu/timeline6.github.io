function exec830(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output830").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor269( newColor ) {
    document.getElementById('b036').style.backgroundColor = newColor;
    }

    function changeBoxColors269( newColor ) {
      document.getElementById('drop71').style.backgroundColor = newColor;
      }
