function exec448(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output448").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor145( newColor ) {
    document.getElementById('c020').style.backgroundColor = newColor;
  }

  function changeBoxColors145( newColor ) {
    document.getElementById('drop89').style.backgroundColor = newColor;
  }
