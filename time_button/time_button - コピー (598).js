function exec598(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output598").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor193( newColor ) {
    document.getElementById('c026').style.backgroundColor = newColor;
  }

  function changeBoxColors193( newColor ) {
    document.getElementById('drop95').style.backgroundColor = newColor;
  }
