function exec788(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output788").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor255( newColor ) {
      document.getElementById('d034').style.backgroundColor = newColor;
    }

    function changeBoxColors255( newColor ) {
      document.getElementById('drop137').style.backgroundColor = newColor;
    }
