function exec824(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output824").innerHTML=ToDay;//指定した場所に表示させる
  }

      function changeBoxColor267( newColor ) {
        document.getElementById('h035').style.backgroundColor = newColor;
        }

        function changeBoxColors267( newColor ) {
          document.getElementById('drop275').style.backgroundColor = newColor;
          }
