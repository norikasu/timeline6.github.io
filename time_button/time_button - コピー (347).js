function exec347(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output347").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor113( newColor ) {
    document.getElementById('a016').style.backgroundColor = newColor;
  }

  function changeBoxColors113( newColor ) {
    document.getElementById('drop16').style.backgroundColor = newColor;
  }
