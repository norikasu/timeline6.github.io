function exec707(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output707").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor228( newColor ) {
      document.getElementById('a031').style.backgroundColor = newColor;
    }

    function changeBoxColors228( newColor ) {
      document.getElementById('drop31').style.backgroundColor = newColor;
    }
