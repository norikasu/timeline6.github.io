function exec197(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output197").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor65( newColor ) {
    document.getElementById('a009').style.backgroundColor = newColor;
  }

  function changeBoxColors65( newColor ) {
    document.getElementById('drop9').style.backgroundColor = newColor;
  }
