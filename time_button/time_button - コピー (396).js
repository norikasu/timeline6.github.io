function exec396(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output396").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor129( newColor ) {
    document.getElementById('b018').style.backgroundColor = newColor;
  }

  function changeBoxColors129( newColor ) {
    document.getElementById('drop53').style.backgroundColor = newColor;
  }
