function exec818(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output818").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor265( newColor ) {
    document.getElementById('f035').style.backgroundColor = newColor;
    }

    function changeBoxColors265( newColor ) {
      document.getElementById('drop204').style.backgroundColor = newColor;
      }
