function exec257(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output257").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor85( newColor ) {
    document.getElementById('a012').style.backgroundColor = newColor;
  }

  function changeBoxColors85( newColor ) {
    document.getElementById('drop12').style.backgroundColor = newColor;
  }
