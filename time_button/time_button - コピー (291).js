function exec291(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output291").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor95( newColor ) {
    document.getElementById('g013').style.backgroundColor = newColor;
  }

  function changeBoxColors95( newColor ) {
    document.getElementById('drop217').style.backgroundColor = newColor;
  }
