function exec649(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output649").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor209( newColor ) {
      document.getElementById('c028').style.backgroundColor = newColor;
    }
