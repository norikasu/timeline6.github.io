function exec748(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output748").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor242( newColor ) {
    document.getElementById('g032').style.backgroundColor = newColor;
  }

  function changeBoxColors242( newColor ) {
    document.getElementById('drop236').style.backgroundColor = newColor;
  }
