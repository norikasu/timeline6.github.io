function exec746(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output746").innerHTML=ToDay;//指定した場所に表示させる
  }

      function changeBoxColor241( newColor ) {
        document.getElementById('f032').style.backgroundColor = newColor;
      }
