function exec552(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output552").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor178( newColor ) {
      document.getElementById('d024').style.backgroundColor = newColor;
    }

    function changeBoxColors178( newColor ) {
      document.getElementById('drop127').style.backgroundColor = newColor;
    }
