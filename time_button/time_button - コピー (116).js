function exec116(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output116").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor39( newColor ) {
    document.getElementById('g005').style.backgroundColor = newColor;
  }

  function changeBoxColors39( newColor ) {
    document.getElementById('drop210').style.backgroundColor = newColor;
  }
