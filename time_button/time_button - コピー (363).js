function exec363(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output363").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor276( newColor ) {
    document.getElementById('g016').style.backgroundColor = newColor;
  }

  function changeBoxColors276( newColor ) {
    document.getElementById('drop220').style.backgroundColor = newColor;
  }
