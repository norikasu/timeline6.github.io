function exec576(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output576").innerHTML=ToDay;//指定した場所に表示させる
  }

        function changeBoxColor186( newColor ) {
          document.getElementById('d025').style.backgroundColor = newColor;
        }

        function changeBoxColors186( newColor ) {
          document.getElementById('drop128').style.backgroundColor = newColor;
        }
