function exec767(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output767").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor248( newColor ) {
    document.getElementById('e033').style.backgroundColor = newColor;
  }

  function changeBoxColors248( newColor ) {
    document.getElementById('drop169').style.backgroundColor = newColor;
  }
