function exec507(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output507").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor163( newColor ) {
    document.getElementById('e022').style.backgroundColor = newColor;
  }

  function changeBoxColors163( newColor ) {
    document.getElementById('drop159').style.backgroundColor = newColor;
  }
