function exec522(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output522").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor168( newColor ) {
      document.getElementById('b023').style.backgroundColor = newColor;
    }

    function changeBoxColors168( newColor ) {
      document.getElementById('drop58').style.backgroundColor = newColor;
    }
