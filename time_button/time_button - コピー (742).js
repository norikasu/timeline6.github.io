function exec742(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output742").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor240( newColor ) {
    document.getElementById('e032').style.backgroundColor = newColor;
  }

  function changeBoxColors240( newColor ) {
    document.getElementById('drop168').style.backgroundColor = newColor;
  }
