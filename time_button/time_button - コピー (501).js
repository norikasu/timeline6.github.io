function exec501(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output501").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor161( newColor ) {
      document.getElementById('c022').style.backgroundColor = newColor;
    }

    function changeBoxColors161( newColor ) {
      document.getElementById('drop91').style.backgroundColor = newColor;
    }
