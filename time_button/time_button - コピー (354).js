function exec354(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output354").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor116( newColor ) {
    document.getElementById('d016').style.backgroundColor = newColor;
  }

  function changeBoxColors116( newColor ) {
    document.getElementById('drop119').style.backgroundColor = newColor;
  }
