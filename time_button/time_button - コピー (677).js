function exec677(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output677").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor219( newColor ) {
      document.getElementById('h029').style.backgroundColor = newColor;
    }

    function changeBoxColors219( newColor ) {
      document.getElementById('drop269').style.backgroundColor = newColor;
    }
