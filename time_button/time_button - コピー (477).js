function exec477(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output477").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor153( newColor ) {
      document.getElementById('c021').style.backgroundColor = newColor;
    }

    function changeBoxColors153( newColor ) {
      document.getElementById('drop90').style.backgroundColor = newColor;
    }
