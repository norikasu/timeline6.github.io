function exec780(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output780").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor252( newColor ) {
      document.getElementById('a034').style.backgroundColor = newColor;
    }

    function changeBoxColors252( newColor ) {
      document.getElementById('drop34').style.backgroundColor = newColor;
    }
