function exec635(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output635").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor205( newColor ) {
      document.getElementById('g027').style.backgroundColor = newColor;
    }

    function changeBoxColors205( newColor ) {
      document.getElementById('drop231').style.backgroundColor = newColor;
    }
