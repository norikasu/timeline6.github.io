function exec638(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output638").innerHTML=ToDay;//指定した場所に表示させる
  }

      function changeBoxColor206( newColor ) {
        document.getElementById('h027').style.backgroundColor = newColor;
      }

      function changeBoxColors206( newColor ) {
        document.getElementById('drop267').style.backgroundColor = newColor;
      }
