function exec812(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output812").innerHTML=ToDay;//指定した場所に表示させる
  }

      function changeBoxColor263( newColor ) {
        document.getElementById('d035').style.backgroundColor = newColor;
      }

      function changeBoxColors263( newColor ) {
        document.getElementById('drop138').style.backgroundColor = newColor;
      }
