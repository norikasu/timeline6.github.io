function exec839(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output839").innerHTML=ToDay;//指定した場所に表示させる
  }

function changeBoxColor272( newColor ) {
  document.getElementById('e036').style.backgroundColor = newColor;
  }

  function changeBoxColors272( newColor ) {
    document.getElementById('drop172').style.backgroundColor = newColor;
    }
