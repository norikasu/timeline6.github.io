function exec755(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output755").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor244( newColor ) {
    document.getElementById('a033').style.backgroundColor = newColor;
  }

  function changeBoxColors244( newColor ) {
    document.getElementById('drop33').style.backgroundColor = newColor;
  }
