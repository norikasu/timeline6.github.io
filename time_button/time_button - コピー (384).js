function exec384(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output384").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor125( newColor ) {
    document.getElementById('f017').style.backgroundColor = newColor;
  }

  function changeBoxColors125( newColor ) {
    document.getElementById('drop187').style.backgroundColor = newColor;
  }
