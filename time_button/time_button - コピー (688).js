function exec688(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output688").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor222( newColor ) {
    document.getElementById('c030').style.backgroundColor = newColor;
  }

  function changeBoxColors222( newColor ) {
    document.getElementById('drop99').style.backgroundColor = newColor;
  }
