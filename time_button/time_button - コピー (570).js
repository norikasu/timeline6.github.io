function exec570(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output570").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor184( newColor ) {
      document.getElementById('b025').style.backgroundColor = newColor;
    }

    function changeBoxColors184( newColor ) {
      document.getElementById('drop60').style.backgroundColor = newColor;
    }
