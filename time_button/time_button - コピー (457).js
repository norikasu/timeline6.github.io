function exec457(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output457").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor148( newColor ) {
    document.getElementById('f020').style.backgroundColor = newColor;
  }

  function changeBoxColors148( newColor ) {
    document.getElementById('drop190').style.backgroundColor = newColor;
  }
