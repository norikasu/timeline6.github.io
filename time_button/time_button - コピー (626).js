function exec626(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output626").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor202( newColor ) {
      document.getElementById('d027').style.backgroundColor = newColor;
    }

    function changeBoxColors202( newColor ) {
      document.getElementById('drop130').style.backgroundColor = newColor;
    }
