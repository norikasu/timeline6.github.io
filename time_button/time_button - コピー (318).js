function exec318(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output318").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor104( newColor ) {
    document.getElementById('h014').style.backgroundColor = newColor;
  }

  function changeBoxColors104( newColor ) {
    document.getElementById('drop254').style.backgroundColor = newColor;
  }
