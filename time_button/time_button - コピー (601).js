function exec601(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output601").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor194( newColor ) {
      document.getElementById('d026').style.backgroundColor = newColor;
    }

    function changeBoxColors194( newColor ) {
      document.getElementById('drop129').style.backgroundColor = newColor;
    }
