function exec809(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output809").innerHTML=ToDay;//指定した場所に表示させる
  }

      function changeBoxColor262( newColor ) {
        document.getElementById('c035').style.backgroundColor = newColor;
      }

      function changeBoxColors262( newColor ) {
        document.getElementById('drop104').style.backgroundColor = newColor;
      }
