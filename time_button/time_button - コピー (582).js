function exec582(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output582").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor188( newColor ) {
      document.getElementById('f025').style.backgroundColor = newColor;
    }

    function changeBoxColors188( newColor ) {
      document.getElementById('drop195').style.backgroundColor = newColor;
    }
