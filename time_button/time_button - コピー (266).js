function exec266(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output266").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor88( newColor ) {
    document.getElementById('h012').style.backgroundColor = newColor;
  }

  function changeBoxColors88( newColor ) {
    document.getElementById('drop252').style.backgroundColor = newColor;
  }
