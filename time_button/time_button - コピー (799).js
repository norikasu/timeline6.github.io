function exec799(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output799").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor258( newColor ) {
      document.getElementById('g034').style.backgroundColor = newColor;
    }
