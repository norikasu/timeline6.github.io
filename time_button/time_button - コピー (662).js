function exec662(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output662").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor214( newColor ) {
    document.getElementById('c029').style.backgroundColor = newColor;
  }

  function changeBoxColors214( newColor ) {
    document.getElementById('drop98').style.backgroundColor = newColor;
  }
