function exec579(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output579").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor187( newColor ) {
    document.getElementById('e025').style.backgroundColor = newColor;
  }

  function changeBoxColors187( newColor ) {
    document.getElementById('drop162').style.backgroundColor = newColor;
  }
