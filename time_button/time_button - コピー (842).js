function exec842(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output842").innerHTML=ToDay;//指定した場所に表示させる
  }

      function changeBoxColor273( newColor ) {
        document.getElementById('f036').style.backgroundColor = newColor;
        }

        function changeBoxColors273( newColor ) {
          document.getElementById('drop205').style.backgroundColor = newColor;
          }
