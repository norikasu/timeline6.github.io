function exec665(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output665").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor215( newColor ) {
      document.getElementById('d029').style.backgroundColor = newColor;
    }

    function changeBoxColors215( newColor ) {
      document.getElementById('drop132').style.backgroundColor = newColor;
    }
