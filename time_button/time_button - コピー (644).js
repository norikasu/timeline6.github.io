function exec644(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output644").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor208( newColor ) {
      document.getElementById('b028').style.backgroundColor = newColor;
    }

    function changeBoxColors208( newColor ) {
      document.getElementById('drop63').style.backgroundColor = newColor;
    }
