function exec516(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output516").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor166( newColor ) {
      document.getElementById('h022').style.backgroundColor = newColor;
    }

    function changeBoxColors166( newColor ) {
      document.getElementById('drop262').style.backgroundColor = newColor;
    }
