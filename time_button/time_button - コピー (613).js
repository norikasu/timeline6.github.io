function exec613(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output613").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor198( newColor ) {
      document.getElementById('h026').style.backgroundColor = newColor;
    }

    function changeBoxColors198( newColor ) {
      document.getElementById('drop266').style.backgroundColor = newColor;
    }
