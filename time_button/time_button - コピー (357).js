function exec357(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output357").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor117( newColor ) {
    document.getElementById('e016').style.backgroundColor = newColor;
  }

  function changeBoxColors117( newColor ) {
    document.getElementById('drop153').style.backgroundColor = newColor;
  }
