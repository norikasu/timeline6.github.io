function exec727(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output727").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor235( newColor ) {
      document.getElementById('h031').style.backgroundColor = newColor;
    }

    function changeBoxColors235( newColor ) {
      document.getElementById('drop271').style.backgroundColor = newColor;
    }
