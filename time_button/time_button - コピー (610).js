function exec610(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output610").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor197( newColor ) {
      document.getElementById('g026').style.backgroundColor = newColor;
    }

    function changeBoxColors197( newColor ) {
      document.getElementById('drop230').style.backgroundColor = newColor;
    }
