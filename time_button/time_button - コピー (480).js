function exec480(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output480").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor154( newColor ) {
      document.getElementById('d021').style.backgroundColor = newColor;
    }

    function changeBoxColors154( newColor ) {
      document.getElementById('drop124').style.backgroundColor = newColor;
    }
