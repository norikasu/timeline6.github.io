function exec845(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output845").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor274( newColor ) {
      document.getElementById('g036').style.backgroundColor = newColor;
      }

      function changeBoxColors274( newColor ) {
        document.getElementById('drop240').style.backgroundColor = newColor;
        }
