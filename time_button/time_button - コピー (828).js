function exec828(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output828").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor268( newColor ) {
      document.getElementById('a036').style.backgroundColor = newColor;
      }

      function changeBoxColors268( newColor ) {
        document.getElementById('drop36').style.backgroundColor = newColor;
        }
