function exec691(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output691").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor223( newColor ) {
      document.getElementById('d030').style.backgroundColor = newColor;
    }

    function changeBoxColors223( newColor ) {
      document.getElementById('drop133').style.backgroundColor = newColor;
    }
