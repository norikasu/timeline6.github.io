function exec785(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output785").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor254( newColor ) {
    document.getElementById('c034').style.backgroundColor = newColor;
  }

  function changeBoxColors254( newColor ) {
    document.getElementById('drop103').style.backgroundColor = newColor;
  }
