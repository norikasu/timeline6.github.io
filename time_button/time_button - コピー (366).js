function exec366(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output366").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor119( newColor ) {
    document.getElementById('h016').style.backgroundColor = newColor;
  }

  function changeBoxColors119( newColor ) {
    document.getElementById('drop256').style.backgroundColor = newColor;
  }
