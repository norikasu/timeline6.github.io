function exec703(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output703").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor227( newColor ) {
      document.getElementById('h030').style.backgroundColor = newColor;
    }

    function changeBoxColors227( newColor ) {
      document.getElementById('drop270').style.backgroundColor = newColor;
    }
