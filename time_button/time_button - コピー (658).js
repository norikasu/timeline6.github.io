function exec658(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output658").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor212( newColor ) {
      document.getElementById('a029').style.backgroundColor = newColor;
    }

    function changeBoxColors212( newColor ) {
      document.getElementById('drop29').style.backgroundColor = newColor;
    }
