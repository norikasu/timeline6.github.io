function exec671(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output671").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor217( newColor ) {
      document.getElementById('f029').style.backgroundColor = newColor;
    }

    function changeBoxColors217( newColor ) {
      document.getElementById('drop198').style.backgroundColor = newColor;
    }
