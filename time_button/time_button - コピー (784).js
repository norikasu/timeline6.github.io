function exec784(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output784").innerHTML=ToDay;//指定した場所に表示させる
  }

      function changeBoxColor253( newColor ) {
        document.getElementById('b034').style.backgroundColor = newColor;
      }
