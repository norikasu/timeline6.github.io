function exec668(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output668").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor216( newColor ) {
    document.getElementById('e029').style.backgroundColor = newColor;
  }

  function changeBoxColors216( newColor ) {
    document.getElementById('drop165').style.backgroundColor = newColor;
  }
