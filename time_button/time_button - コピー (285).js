function exec285(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output285").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor93( newColor ) {
    document.getElementById('e013').style.backgroundColor = newColor;
  }

  function changeBoxColors93( newColor ) {
    document.getElementById('drop150').style.backgroundColor = newColor;
  }
