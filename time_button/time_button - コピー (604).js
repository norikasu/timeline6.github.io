function exec604(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output604").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor195( newColor ) {
    document.getElementById('e026').style.backgroundColor = newColor;
  }

  function changeBoxColors195( newColor ) {
    document.getElementById('drop163').style.backgroundColor = newColor;
  }
