function exec568(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output568").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor183( newColor ) {
      document.getElementById('a025').style.backgroundColor = newColor;
    }

    function changeBoxColors183( newColor ) {
      document.getElementById('drop25').style.backgroundColor = newColor;
    }
