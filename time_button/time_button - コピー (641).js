function exec641(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output641").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor207( newColor ) {
    document.getElementById('a028').style.backgroundColor = newColor;
  }

  function changeBoxColors207( newColor ) {
    document.getElementById('drop28').style.backgroundColor = newColor;
  }
