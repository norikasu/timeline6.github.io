function exec137(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output137").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor46( newColor ) {
    document.getElementById('f006').style.backgroundColor = newColor;
  }

  function changeBoxColors46( newColor ) {
    document.getElementById('drop178').style.backgroundColor = newColor;
  }
