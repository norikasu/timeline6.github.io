function exec683(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output683").innerHTML=ToDay;//指定した場所に表示させる
  }

    function changeBoxColor220( newColor ) {
      document.getElementById('a030').style.backgroundColor = newColor;
    }

    function changeBoxColors220( newColor ) {
      document.getElementById('drop30').style.backgroundColor = newColor;
    }
