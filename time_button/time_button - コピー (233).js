function exec233(){
  var today=new Date();//今日の日付を取得
  var ToDay=(today.getMonth()+1)+"/"+today.getDate()+" "+today.getHours()+":"+today.getMinutes();//表示する内容
  document.getElementById("output233").innerHTML=ToDay;//指定した場所に表示させる
  }

  function changeBoxColor77( newColor ) {
    document.getElementById('a011').style.backgroundColor = newColor;
  }

  function changeBoxColors77( newColor ) {
    document.getElementById('drop11').style.backgroundColor = newColor;
  }
